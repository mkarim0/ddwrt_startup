A simple script to limit daily usage of WAN on ddwrt router

Usage:

Paste the contents of startup.sh into Administration/Commands , then save as startup
change the value 4710 to the prefered value (in MB)
